services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      GEOATM_DATA_DIR: /data
      GEOATM_AUTO_INGEST: "1"
      GEOATM_CORS_ORIGINS: "*"
    volumes:
      - ..:/data:ro

  web:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - api
